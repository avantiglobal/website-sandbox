/**
 * Tier 3 patterns barrel (spec 0.5). Structural variants, zero content
 * knowledge — blocks pick a pattern via a CMS `variant` field.
 */
export { Split } from "./split";
export type { SplitProps } from "./split";
export { Center } from "./center";
export type { CenterProps } from "./center";
