/**
 * Site chrome (spec 0.7). Header and footer read their content from site.yml
 * via the settings reader — never hardcoded — so navigation stays editable.
 */
export { Header } from "./header";
export { Footer } from "./footer";
